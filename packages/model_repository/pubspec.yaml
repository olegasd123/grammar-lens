name: model_repository
description: Model download, storage, integrity verification, and registry for GrammarLens.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  http: ^1.2.2
  logging: ^1.3.0
  meta: ^1.16.0
  path: ^1.9.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

resolution: workspace
